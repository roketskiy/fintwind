1. Add an explicit, idempotent shutdown operation to `waku_client::DaemonSupervisor`. It will first stop its rebuild monitor, then gracefully stop only its `Local(DaemonProcess)` target using the existing protocol shutdown, bounded wait, and forced-child cleanup fallback. A `Remote` target remains untouched, preserving the documented contract for externally managed daemons.

2. Register one additional `cx.on_app_quit` callback during `Waku` construction in `src/app.rs`. The callback will clone the supervisor and run its blocking shutdown work through GPUI’s background executor, returning the task future so a genuine application exit waits for cleanup without blocking rendering.

3. Keep all window-close-to-tray behavior unchanged. Cleanup runs only after `cx.quit()`, covering Quit menu/shortcut/tray exit and Linux’s final-window exit, while ordinary Windows/macOS close continues to hide the app and leave sessions/backends running.

4. Add focused unit coverage in the client process module for the ownership rule where practical, then run formatting and the smallest relevant Rust test set. The existing daemon/OpenCode shutdown cascade will be retained: shutting down the local daemon drops session drivers and terminates their `opencode2 serve` children, including the Windows process tree fallback.