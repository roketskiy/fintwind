将撤销上一版“右侧按供应商分组”的呈现，改为真正的供应商侧栏筛选。

1. 把 `ModelPickerTab` 从固定的 `Favorites | Provider` 改为 `Favorites | Provider(String)`，字符串为规范化后的供应商名称。

2. 从已安装模型的 `ProviderModel.sub_provider` 动态生成左侧供应商列表：
   - 顺序按供应商首次出现确定，保证稳定。
   - `None`、空白值和大小写不同的 `OpenCode` 合并为 `OpenCode`。
   - 收藏固定置顶；其下的供应商区域独立滚动，防止供应商过多时溢出固定高度的弹窗。
   - 选中的供应商用现有的 `theme.accent`，即 `#D97757`，作为图标/文字与低透明度选中底色。非 OpenCode 供应商使用现有通用图标，并为图标提供供应商名称提示。

3. 右侧列表恢复为平铺模型行，不再显示供应商分组标题：
   - 未搜索时，`Favorites` 显示收藏模型并保留用户收藏顺序；某个 `Provider(name)` 只显示该供应商的模型。
   - 搜索时仍在全部已安装模型中匹配名称、模型 ID 与供应商名，忽略左侧当前分类；左侧不显示选中态。
   - 模型行保留模型 ID 作为次要信息；不重复展示供应商标题。

4. 让打开和选中模型后的默认侧栏指向该模型所属供应商，而不是已删除的“全部 OpenCode”分类。切换供应商时继续刷新目录、清空键盘高亮并定位当前模型；当收藏页没有当前模型时保持现有的回到列表顶部行为。

5. 维持键盘与滚动一致性：
   - 上下键、Enter、搜索后首项定位、切换供应商后的定位，都使用同一份“当前侧栏或当前搜索结果”的模型序列。
   - 移除上一版为右侧供应商标题引入的分组重排和滚动偏移映射，因为右侧不再有非选择性标题项。
   - `Tab` / `Shift+Tab` 在收藏和所有动态供应商之间循环；搜索时继续禁用分类循环。

6. 更新模型选择器的纯逻辑测试，覆盖动态供应商列表顺序、供应商筛选、OpenCode 回退归类、收藏顺序、跨供应商搜索和动态 Tab 环绕；保留已有的上下键和 Enter 语义覆盖。

7. 完成后运行改动文件格式检查、模型选择器目标测试、完整 `app::tests` 测试集和 `cargo check -p fintwind`。不手动启动或重启现有开发 watcher，也不进行未请求的视觉测试。