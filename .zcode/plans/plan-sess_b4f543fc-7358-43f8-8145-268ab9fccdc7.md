1. 在 `src/app/sidebar.rs` 中，将侧栏底部的 `open-settings` 从纯图标按钮改为带齿轮图标和“设置”文字的圆角矩形操作按钮。按钮采用约 `32 px` 高度、`8 px` 圆角、适当左右内边距与 `15 px` 图标，保留现有焦点环、提示、点击处理和底栏布局。

2. 在 `src/app/window_chrome.rs` 为 Windows 分支定义连续的标题栏按钮组：最小化、最大化/还原、关闭均使用 `46x32 px` 的矩形命中区，组容器不保留横向内边距与间隔，且不使用圆角。图标保持适合标题栏的约 `12 px` 尺寸。

3. 仅在 Windows 平台采用这套标题栏样式；macOS 的现有表现不变。保留每个控件的 `WindowControlArea::{Min, Max, Close}`，因此 Windows 标题栏命中测试、最大化按钮的 Snap Layout、拖拽区域、键盘操作以及关闭时最小化至托盘的行为不受影响。

4. 完成后运行 `cargo check` 和 `git diff --check`。不启动第二个开发 watcher，也不进行未请求的视觉验证。